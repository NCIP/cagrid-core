The 'remote' package is intended for tests using a remote deployment of the Federatated Query service.  Tests using the 'local' client side API of the federated query engine belong in the sibling 'local' package.