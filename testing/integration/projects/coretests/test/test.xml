<!-- ======================================================================= -->
<!-- caGrid Utilities build file                                             -->
<!-- ======================================================================= -->
<project name="caGrid-tests-core-test" basedir=".">
	<property name="junit.results.dir" value="${test.log.dir}/junit" />
	<property name="force.config.replace" value="false" />


	<!-- call the test target for each project and aggregates the log resutls  -->
	<target name="test">
		<property name="test.pattern" value="*Test" />

		<echo message="Test results dir: ${junit.results.dir}" />
		<echo message="Test source dir: ${test.src.dir}" />
		<mkdir dir="${junit.results.dir}" />
		<junit dir="${basedir}" printsummary="yes" haltonfailure="false" haltonerror="false" showoutput="true">
			<sysproperty key="basedir" value="${basedir}" />

			<jvmarg value="-Dbuild.dir=${build.dir}" />
			<jvmarg value="-Djava.endorsed.dirs=${ext.endorsed.dir}" />
			<jvmarg value="-Djunit.results.dir=${junit.results.dir}" />

			<classpath>
				<path refid="test.classpath" />
			</classpath>

			<formatter type="xml" usefile="true" />
			<formatter type="plain" usefile="false" />

			<batchtest fork="yes" haltonfailure="false" haltonerror="false" todir="${junit.results.dir}" failureproperty="test.failed" errorproperty="test.error">
				<fileset dir="${test.src.dir}">
					<exclude name="**/*Abstract*.java" />
					<exclude name="**/WorkflowTest.java" />
					<!-- <exclude name="**/BasicDataServiceTest.java" />-->
					<include name="**/${test.pattern}.java" />
					<exclude name="**/IndexServiceTest.java"/>
					<!-- <include name="**/*TestCase.java" /> -->
				</fileset>
			</batchtest>
		</junit>
		<fail message="Tests failed! Consult the logs for details." if="test.failed" />
		<fail message="Tests errored! Consult the logs for details." if="test.errored" />
	</target>


	<target name="testReport" depends="test" description="Runs the tests and generates a test report.">

		<mkdir dir="${junit.results.dir}/report" />
		<junitreport todir="${junit.results.dir}">
			<fileset dir="${junit.results.dir}">
				<include name="TEST-*.xml" />
			</fileset>
			<report format="frames" todir="${junit.results.dir}/report" />
		</junitreport>
		<echo message="Created test report in ${junit.results.dir}/report" />

	</target>
</project>
