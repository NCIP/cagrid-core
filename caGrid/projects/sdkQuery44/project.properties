target.cagrid.version=1.4
# project.version needs to match target.cagrid.version
# we do this because otherwise we'd have to make changes 
# to our dependencies-build.xml to redefine the artifact 
# pattern Ivy looks for
project.version=1.4
