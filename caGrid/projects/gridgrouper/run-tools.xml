<project name="run-tools file" basedir="." default="runClient">
     <target name="runClient" depends="init" description="Run the sample Client">
        <property name="service.url" value="https://localhost:8443/wsrf/services/cagrid/GridGrouper" />
        <echo message="Connecting to service: ${service.url}" />
        <java failonerror="true" classname="gov.nih.nci.cagrid.gridgrouper.client.GridGrouperClient" classpathref="run.classpath" fork="yes">
            <jvmarg value="-DGLOBUS_LOCATION=${ext.globus.dir}" />
            <arg value="-url" />
            <arg value="${service.url}" />
        </java>
    </target>
</project>
