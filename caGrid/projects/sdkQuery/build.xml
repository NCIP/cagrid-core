<!-- ======================================================================= -->
<!-- caGrid 1.0 SDK Query Processor build file                               -->
<!-- ======================================================================= -->
<project name="caGrid-sdkQuery" default="all" basedir=".">
	<!-- Environment -->
	<property environment="env" />
	<property file="${basedir}/project.properties" />
	<property file="${basedir}/build.properties" />
	<property name="globus.dir" location="${env.GLOBUS_LOCATION}" />

	<!-- build with debugging information -->
	<property name="java.debug" value="on" />
	<!-- enforce java 1.5 compliance at build time -->
	<property name="java.source" value="1.5" />

	<!-- Information -->
	<property name="project.name" value="${ant.project.name}" />
	<property name="project.jar.prefix" value="${project.name}-" />

	<!-- build output dirs -->
	<property name="build.dir" location="build" />
	<property name="build.dest" location="${build.dir}/classes" />
	<property name="build.jars.dir" location="build/lib" />
	
	<!-- style stuff -->
	<property name="introduce.extensions.dir" location="../introduce/extensions"/>
	<property name="style.dest.dir" location="${introduce.extensions.dir}/data/styles/cacore31" />
	<property name="style.document" location="${basedir}/style/style.xml"/>

	<!-- jar names -->
	<property name="project.jar.name" value="${project.jar.prefix}core-${project.version}.jar" />

	<!-- jar files -->
	<property name="project.jar.file" value="${build.jars.dir}/${project.jar.name}" />

	<!-- source directories -->
	<property name="src.dir" location="${basedir}/src/java" />

	<!-- libraries -->
	<property name="ext.lib.dir" location="${basedir}/ext/dependencies/jars" />
	<property name="ext.test.lib.dir" location="${basedir}/ext/dependencies/test/jars" />

	<!-- testing stuff -->
	<property name="test.dir" location="${basedir}/test" />
	<property name="test.src.dir" location="${test.dir}/src/java" />
	<property name="test.classes.dir" location="${build.dir}/test/classes" />
	<property name="test.logs.dir" location="${test.dir}/logs" />
	<property name="test.project.jar" location="${build.jars.dir}/${project.jar.prefix}tests-${project.version}.jar" />
	<import file="test/test.xml" />

	<!-- =============================================================== -->
	<!-- The Test Classpath                                              -->
	<!-- =============================================================== -->
	<path id="test.classpath">
		<pathelement path="${build.dest}" />
		<fileset dir="${ext.lib.dir}">
			<include name="**/*.jar" />
			<!-- this is here until the authz project fixes their build artifacts -->
			<exclude name="asm.jar" />
		</fileset>
		<fileset dir="${ext.test.lib.dir}">
			<include name="**/*.jar" />
		</fileset>
		<fileset dir="${globus.dir}/lib">
			<include name="**/*.jar" />
            <exclude name="junit.jar"/>
		</fileset>
	</path>

	<!-- creates output directories -->
	<target name="init">
		<mkdir dir="${build.dir}" />
		<mkdir dir="${build.dest}" />
		<mkdir dir="${build.jars.dir}" />
		<mkdir dir="${test.classes.dir}" />
	</target>

	<!-- compiles the query processor source code -->
	<target name="compile" depends="init">
		<javac srcdir="${src.dir}" destdir="${build.dest}" debug="${java.debug}" source="${java.source}">
			<classpath>
				<fileset dir="${ext.lib.dir}">
					<include name="*.jar" />
				</fileset>
				<fileset dir="${globus.dir}/lib">
					<include name="*.jar" />
                    <exclude name="junit.jar"/>
				</fileset>
			</classpath>
			<include name="**/*.java" />
		</javac>
	</target>

	<!-- compiles the testing source code -->
	<target name="compileTests" depends="compile">
		<javac srcdir="${test.src.dir}" destdir="${test.classes.dir}" debug="${java.debug}" source="${java.source}">
			<classpath>
				<pathelement path="${build.dest}" />
				<fileset dir="${ext.lib.dir}">
					<include name="*.jar" />
				</fileset>
				<fileset dir="${ext.test.lib.dir}">
					<include name="*.jar" />
				</fileset>
				<fileset dir="${globus.dir}/lib">
					<include name="*.jar" />
                    <exclude name="junit.jar"/>
				</fileset>
			</classpath>
			<include name="**/*.java" />
		</javac>
	</target>

	<!-- jars the classes -->
	<target name="jar" depends="compile" description="Builds the jar file">
		<jar destfile="${project.jar.file}">
			<fileset dir="${build.dest}">
				<include name="**/*.class" />
			</fileset>
			<fileset dir="${src.dir}">
				<include name="**/*" />
			</fileset>
		</jar>
	</target>

	<!-- jars the tests -->
	<target name="jarTests" depends="compileTests" description="Builds the testing jar file">
		<jar destfile="${test.project.jar}">
			<fileset dir="${test.classes.dir}">
				<include name="**/*.class" />
			</fileset>
		</jar>
	</target>
	
	
	<!-- installs the service style -->
	<target name="installServiceStyle" depends="jar">
		<!-- make requisite directories -->
		<delete dir="${style.dest.dir}" />
		<mkdir dir="${style.dest.dir}" />
		<mkdir dir="${style.dest.dir}/lib" />
	
		<!-- set up the extension lib dir -->
		<property name="sdk31.extension.lib.dir" location="${introduce.extensions.dir}/data/sdk31/lib"/>
		<delete dir="${sdk31.extension.lib.dir}"/>
		<mkdir dir="${sdk31.extension.lib.dir}"/>
		<copy todir="${sdk31.extension.lib.dir}">
			<!-- sdk31 query processor library -->
			<fileset dir="${build.jars.dir}">
				<include name="${project.jar.name}"/>
			</fileset>
		</copy>
		<copy todir="${style.dest.dir}/lib">
			<!-- sdk31 query processor library -->
			<fileset dir="${build.jars.dir}">
				<include name="${project.jar.name}"/>
			</fileset>
			<!-- sdkstyle library -->
			<fileset dir="${ext.lib.dir}">
				<include name="*sdkstyle*.jar"/>
			</fileset>
		</copy>
	
		<!-- copy in the style.xml file -->
		<copy file="${style.document}" todir="${style.dest.dir}" />
	</target>
	

	<target name="installIntroduceExtensions" depends="installServiceStyle" description="Builds and installs the SDK 31 data service style into Introduce.">
	</target>

	<!-- removes all build output -->
	<target name="clean">
		<delete dir="${build.dir}" />
	</target>

	<!-- build and jar everything -->
	<target name="all" depends="jar, jarTests" description="Builds and jars all the classes in the project" />
</project>
